# Database
DATABASE_URL="postgresql://user:password@localhost:5432/cva_policy_db"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Optional: Email (configure in Admin > Notifications)
# SMTP settings are stored in the database and configured via the admin UI
